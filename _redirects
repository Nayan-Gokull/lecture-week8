# Cloudflare Pages redirect rules.
#
# There is no build step, so every file in this repo is served publicly.
# Send documentation and raw data paths back to the hub instead of letting
# students land on them by guessing a URL.
#
# Format: /source /destination [status]

/README.md      /source.html   302
/README         /source.html   302
/TEACHING.md    /source.html   302
/TEACHING       /source.html   302
/data/          /source.html   302
/panels/        /source.html   302
